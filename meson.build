project('physx', ['cpp', 'c'],
  version: '5.5.1')

#physxInitScript = join_paths(meson.source_root(), 'physx/generate_projects.sh')

#initCMake = run_command('bash', physxInitScript, 'linux-gcc')

#output = initCMake.stdout().strip()
#message(output)
message('hi')

subdir('physx')
